import { useEffect, useState } from 'react';

export default function ComponentsPC() {
  // Borré lo poco que había acá para evitar problemas por ahora
  return (
    <p slot="fallback" id="no-search-text" className="px-4 max-w-xl mx-auto text-[#aaaa] mt-4">Por ahora puedes buscar procesadores de Intel, con el tiempo iré añadiendo más componentes.</p>
  )
}